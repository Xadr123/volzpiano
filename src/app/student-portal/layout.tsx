import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Student Portal | Volz Method Piano Lessons",
  description: "Access your lesson materials, track progress, and stay connected with your Volz Piano instructor.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
