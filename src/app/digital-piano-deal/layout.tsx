import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Digital Piano Deal | Volz Method Piano Lessons",
  description: "Get the recommended digital piano setup for your child's lessons. Budget and upgraded options curated by Volz Piano teachers.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
